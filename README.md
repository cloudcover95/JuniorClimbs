# JuniorClimbs (Public Open Source Reference)

Maintenance recommender is now event-aware (avoids heavy work during peak yoga/crossfit times).
Future BitNet 3.0 versions can replace the rule-based logic with manifold-powered predictive models while keeping the same interface.