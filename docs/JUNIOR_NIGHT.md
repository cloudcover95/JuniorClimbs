# Overnight BitNet for JuniorStoneField

Field conditions text → JuniorLLM DreamMesh seed → morning access/condition brief.

Hook: copy last ForumMesh bodies into STATE.md then run JuniorLLM `bitnet_night.cycle.run_cycle`.
Covenant still blocks private-land publish overnight — jobs with injection strings are `blocked`.
